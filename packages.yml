packages:

  - package: dbt-labs/dbt_utils # This dbt package contains macros that can be (re)used across dbt projects.
    version: [">=1.0.0", "<2.0.0"] # https://hub.getdbt.com/dbt-labs/dbt_utils/latest
