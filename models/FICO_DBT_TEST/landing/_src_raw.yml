version: 2

sources:
  - name: raw
    config:
      tags: ["nightly"]
    description: ""
    database:  DEV_FICO
    schema: RAW_SCH
    tables:
      - name: customers
        description: "customer data"
      - name: orders
        description: "order data"